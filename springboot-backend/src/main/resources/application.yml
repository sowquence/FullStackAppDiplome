#DataBase
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/students_management_system?useSSL=false
    username: root
    password: root

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update